# CNN_Autoencoder
A Google Colab face-classification pipeline using a CNN autoencoder with Local Contrast Normalization (LCN) for feature extraction and an SVM tuned with GridSearchCV. It loads images from Drive, trains the autoencoder, extracts features, evaluates accuracy, visualizes predictions, and saves models and results.
